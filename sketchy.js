var 









// just called once and done
function setup() {
    inp = creatInput('', "text")
    inp.position(10, 5+140)
    inp.size(10*20, 90);
 
    createCanvas(windowWidth,windowHeight);   
}
//Draw runs over and over again
 function draw() {
     background("red")
     
}
  
       